-- vim-sleuth: auto-detect indentation style (tab vs spaces, tab width) from
-- the content of the current file and neighbouring files in the same directory.
-- Silently sets tabstop / shiftwidth / expandtab — no manual configuration needed.

return { "tpope/vim-sleuth" }
